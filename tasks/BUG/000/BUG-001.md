---
title: When creaing project with empty description. there is not validation feedback. nothing happens
status: done
priority: high
---

# Description

it seems we just return without doing anyting. instead should show a dialog

`src/pages/projects/projects.handlers.js`

```js
    // Validate input
    if (!name || !description || !projectPath) {
      return;
    }
```

