componentName: rvn-command-line-actions
propsSchema:
  type: object
  properties:
    actionsType:
      type: string
      enum:
        - presentation
        - system
    hiddenModes:
      type: array
      items:
        type: string
    allowedModes:
      type: array
      items:
        type: string
