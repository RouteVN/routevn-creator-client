onMount:
  handler: handleAfterMount
refs:
  canvas:
    selector: canvas
template:
  - rtgl-view w=${w} h=${h}:
      - $if waveformData:
          - rtgl-waveform :waveformData=${waveformData} w=${w} h=${h}: null
