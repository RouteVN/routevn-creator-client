componentName: rvn-system-actions
propsSchema:
  type: object
  properties:
    actions:
      type: object
    selectedLineId:
      type: string
    selectedLine:
      type: object
    currentSceneId:
      type: string
    presentationState:
      type: object
    actionType:
      type: string
      enum:
        - presentation
        - system
    showSelected:
      type: boolean
