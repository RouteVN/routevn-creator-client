componentName: rvn-image-selector
propsSchema:
  type: object
  properties:
    selectedImageId:
      type: string
    searchQuery:
      type: string
    columns:
      type: integer
      minimum: 1
