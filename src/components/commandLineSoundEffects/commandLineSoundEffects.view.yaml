elementName: rvn-command-line-sound-effects

viewDataSchema:
  type: object
  properties:
    mode:
      type: string
      enum: ["current", "gallery"]
    items:
      type: array
    groups:
      type: array
    sfx:
      type: array
    tempSelectedResourceId:
      type: string
    breadcrumb:
      type: array
      items:
        type: object
        properties:
          id:
            type: string
          label:
            type: string
    form:
      type: object
    defaultValues:
      type: object
    dropdownMenu:
      type: object
      properties:
        isOpen:
          type: boolean
        position:
          type: object
          properties:
            x:
              type: number
            y:
              type: number
        items:
          type: array
        sfxId:
          type: string

propsSchema:
  type: object
  properties:
    sfx:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              resourceId:
                type: string

refs:
  add-new-button:
    eventListeners:
      click:
        handler: handleAddNewClick
  sfx-*:
    eventListeners:
      click:
        handler: handleSfxClick
      contextmenu:
        handler: handleSfxContextMenu
  resource-item-*:
    eventListeners:
      click:
        handler: handleResourceItemClick
  submit-button:
    eventListeners:
      click:
        handler: handleSubmitClick
  breadcumb:
    eventListeners:
      item-click:
        handler: handleBreadcumbClick
  button-select:
    eventListeners:
      click:
        handler: handleButtonSelectClick
  form:
    eventListeners:
      extra-event:
        handler: handleFormExtra
      form-change:
        handler: handleFormChange
  dropdown-menu:
    eventListeners:
      close:
        handler: handleDropdownMenuClose
      click-item:
        handler: handleDropdownMenuClickItem

template:
  - rtgl-view w=f h=f p=lg:
      - rtgl-view d=h g=md:
      - rtgl-breadcrumb#breadcumb .items=breadcrumb:
      - $if mode == 'current':
          - rtgl-form#form .form=form .defaultValues=defaultValues:
              - rtgl-view#sfx-container slot=sfx g=md:
                  - $for sfx in defaultValues.sfx:
                      - rtgl-view g=sm:
                          - rtgl-view#sfx-${sfx.id} cur=p:
                              - rvn-waveform-visualizer waveformDataFileId=${sfx.waveformDataFileId} width=355 height=150:
                          - rtgl-text w=355 ellipsis=true s=sm c=mu-fg: ${sfx.name}
                  - rtgl-button#add-new-button v=ol w=f mt=md: + Add New Sound Effect
          - rtgl-view flex=1:
          - rtgl-view d=h av=c ah=e w=f g=lg:
              - rtgl-button#submit-button: Submit

      - $if mode == 'gallery':
          - 'rtgl-view d=h w=f g=lg mt=sm flex=1 style="min-height: 0;"':
              - rtgl-view h=f flex=1 sv:
                  - rvn-base-file-explorer#base-file-explorer .items=items no-empty-message:
              - rtgl-view h=f flex=2 sv g=lg:
                  - $for group in groups:
                      - rtgl-view g=md:
                          - rtgl-text: ${group.name}
                          - rtgl-view d=h w=f fw=w g=lg:
                              - $for item in group.children:
                                  - rtgl-view#resource-item-${item.id} cur=p bw=${item.bw} bc=${item.bc} g=sm:
                                      - $if item.waveformDataFileId:
                                          - rvn-waveform-visualizer waveformDataFileId=${item.waveformDataFileId} width=225 h=120:
                                          - rtgl-text s=sm ah=c: ${item.name}
                                        $else:
                                          - rtgl-view w=225 h=120 av=c ah=c bgc=ac:
                                              - rtgl-text s=sm c=mu-fg: No waveform
          - rtgl-view d=h av=c ah=e w=f g=lg:
              - rtgl-button#button-select: Select

  - rtgl-dropdown-menu#dropdown-menu .items=dropdownMenu.items x=${dropdownMenu.position.x} y=${dropdownMenu.position.y} ?open=${dropdownMenu.isOpen}:
