refs:
  image: {}
template:
  - $if hasSrc:
      - rtgl-image#image src=${src} bc=${borderColor} of=con ${containerAttrString} ondragstart="event.preventDefault()": null
    $else:
      - rtgl-view#image bc=${borderColor} ${containerAttrString}: null
