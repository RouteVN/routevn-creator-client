componentName: rvn-square-image-cropper
propsSchema:
  type: object
  properties:
    file:
      type: object
      description: The selected source image file to crop
    outputSize:
      type: number
      description: Optional fixed pixel size for the cropped square output
methods:
  type: object
  properties:
    getCroppedFile:
      description: Returns a square-cropped image file based on the current crop state.
      params: []
      returns: object
