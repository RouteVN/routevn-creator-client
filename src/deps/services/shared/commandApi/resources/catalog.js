import { COMMAND_TYPES } from "../../../../../internal/project/commands.js";
import {
  submitCreateResourceCommand,
  submitDeleteResourceCommand,
  submitMoveResourceCommand,
  submitUpdateResourceCommand,
} from "./shared.js";

export const createCatalogResourceCommandApi = (shared) => ({
  createAnimation: async ({
    animationId,
    data,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitCreateResourceCommand({
      shared,
      resourceType: "animations",
      type: COMMAND_TYPES.ANIMATION_CREATE,
      idField: "animationId",
      idValue: animationId,
      data,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  updateAnimation: async ({ animationId, data }) =>
    submitUpdateResourceCommand({
      shared,
      resourceType: "animations",
      type: COMMAND_TYPES.ANIMATION_UPDATE,
      idField: "animationId",
      idValue: animationId,
      data,
    }),
  moveAnimation: async ({
    animationId,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitMoveResourceCommand({
      shared,
      resourceType: "animations",
      type: COMMAND_TYPES.ANIMATION_MOVE,
      idField: "animationId",
      idValue: animationId,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  deleteAnimations: async ({ animationIds }) =>
    submitDeleteResourceCommand({
      shared,
      resourceType: "animations",
      type: COMMAND_TYPES.ANIMATION_DELETE,
      deleteField: "animationIds",
      ids: animationIds,
    }),
  createCharacter: async ({
    characterId,
    data,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitCreateResourceCommand({
      shared,
      resourceType: "characters",
      type: COMMAND_TYPES.CHARACTER_CREATE,
      idField: "characterId",
      idValue: characterId,
      data,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  updateCharacter: async ({ characterId, data }) =>
    submitUpdateResourceCommand({
      shared,
      resourceType: "characters",
      type: COMMAND_TYPES.CHARACTER_UPDATE,
      idField: "characterId",
      idValue: characterId,
      data,
    }),
  moveCharacter: async ({
    characterId,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitMoveResourceCommand({
      shared,
      resourceType: "characters",
      type: COMMAND_TYPES.CHARACTER_MOVE,
      idField: "characterId",
      idValue: characterId,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  deleteCharacters: async ({ characterIds }) =>
    submitDeleteResourceCommand({
      shared,
      resourceType: "characters",
      type: COMMAND_TYPES.CHARACTER_DELETE,
      deleteField: "characterIds",
      ids: characterIds,
    }),
  createTransform: async ({
    transformId,
    data,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitCreateResourceCommand({
      shared,
      resourceType: "transforms",
      type: COMMAND_TYPES.TRANSFORM_CREATE,
      idField: "transformId",
      idValue: transformId,
      data,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  updateTransform: async ({ transformId, data }) =>
    submitUpdateResourceCommand({
      shared,
      resourceType: "transforms",
      type: COMMAND_TYPES.TRANSFORM_UPDATE,
      idField: "transformId",
      idValue: transformId,
      data,
    }),
  moveTransform: async ({
    transformId,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitMoveResourceCommand({
      shared,
      resourceType: "transforms",
      type: COMMAND_TYPES.TRANSFORM_MOVE,
      idField: "transformId",
      idValue: transformId,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  deleteTransforms: async ({ transformIds }) =>
    submitDeleteResourceCommand({
      shared,
      resourceType: "transforms",
      type: COMMAND_TYPES.TRANSFORM_DELETE,
      deleteField: "transformIds",
      ids: transformIds,
    }),
  createColor: async ({
    colorId,
    data,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitCreateResourceCommand({
      shared,
      resourceType: "colors",
      type: COMMAND_TYPES.COLOR_CREATE,
      idField: "colorId",
      idValue: colorId,
      data,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  updateColor: async ({ colorId, data }) =>
    submitUpdateResourceCommand({
      shared,
      resourceType: "colors",
      type: COMMAND_TYPES.COLOR_UPDATE,
      idField: "colorId",
      idValue: colorId,
      data,
    }),
  moveColor: async ({
    colorId,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitMoveResourceCommand({
      shared,
      resourceType: "colors",
      type: COMMAND_TYPES.COLOR_MOVE,
      idField: "colorId",
      idValue: colorId,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  deleteColors: async ({ colorIds }) =>
    submitDeleteResourceCommand({
      shared,
      resourceType: "colors",
      type: COMMAND_TYPES.COLOR_DELETE,
      deleteField: "colorIds",
      ids: colorIds,
    }),
  createTextStyle: async ({
    textStyleId,
    data,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitCreateResourceCommand({
      shared,
      resourceType: "textStyles",
      type: COMMAND_TYPES.TEXTSTYLE_CREATE,
      idField: "textStyleId",
      idValue: textStyleId,
      data,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  updateTextStyle: async ({ textStyleId, data }) =>
    submitUpdateResourceCommand({
      shared,
      resourceType: "textStyles",
      type: COMMAND_TYPES.TEXTSTYLE_UPDATE,
      idField: "textStyleId",
      idValue: textStyleId,
      data,
    }),
  moveTextStyle: async ({
    textStyleId,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitMoveResourceCommand({
      shared,
      resourceType: "textStyles",
      type: COMMAND_TYPES.TEXTSTYLE_MOVE,
      idField: "textStyleId",
      idValue: textStyleId,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  deleteTextStyles: async ({ textStyleIds }) =>
    submitDeleteResourceCommand({
      shared,
      resourceType: "textStyles",
      type: COMMAND_TYPES.TEXTSTYLE_DELETE,
      deleteField: "textStyleIds",
      ids: textStyleIds,
    }),
  createVariable: async ({
    variableId,
    data,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitCreateResourceCommand({
      shared,
      resourceType: "variables",
      type: COMMAND_TYPES.VARIABLE_CREATE,
      idField: "variableId",
      idValue: variableId,
      data,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  updateVariable: async ({ variableId, data }) =>
    submitUpdateResourceCommand({
      shared,
      resourceType: "variables",
      type: COMMAND_TYPES.VARIABLE_UPDATE,
      idField: "variableId",
      idValue: variableId,
      data,
    }),
  moveVariable: async ({
    variableId,
    parentId,
    position = "last",
    positionTargetId,
    index,
  }) =>
    submitMoveResourceCommand({
      shared,
      resourceType: "variables",
      type: COMMAND_TYPES.VARIABLE_MOVE,
      idField: "variableId",
      idValue: variableId,
      parentId,
      position,
      positionTargetId,
      index,
    }),
  deleteVariables: async ({ variableIds }) =>
    submitDeleteResourceCommand({
      shared,
      resourceType: "variables",
      type: COMMAND_TYPES.VARIABLE_DELETE,
      deleteField: "variableIds",
      ids: variableIds,
    }),
});
