elementName: rvn-layout-editor

viewDataSchema:
  type: object
  properties:
    flatItems:
      type: array
    flatGroups:
      type: array
    selectedItemId:
      type: string
    repositoryTarget:
      type: string
    detailTitle:
      type: string
    detailFields:
      type: array
    detailEmptyMessage:
      type: string

refs:
  file-explorer:
    eventListeners:
      targetchanged:
        required: true
        handler: handleTargetChanged
      click-item:
        handler: handleFileExplorerItemClick
      data-changed:
        handler: handleDataChanged
  groupview:
    eventListeners:
      layout-add-click:
        handler: handleAddLayoutClick
  dialogue-form:
    eventListeners:
      form-change:
        handler: handleDialogueFormChange
  choice-form:
    eventListeners:
      form-change:
        handler: handleChoiceFormChange
  back-button:
    eventListeners:
      click:
        handler: handleBackClick
  layout-edit-panel:
    eventListeners:
      update:
        handler: handleLayoutEditPanelUpdateHandler

template:
  - rtgl-view d=h w=f h=f:
      - rtgl-view d=h h=f:
          - rvn-resizable-panel w=300 min-w=200 max-w=500 resize-side="right":
              - rtgl-view slot="content" w=f h=f:
                  - rvn-resource-types .resourceCategory=resourceCategory .selectedResourceId=selectedResourceId:
                  - rvn-file-explorer#file-explorer .items=flatItems .repositoryTarget=repositoryTarget .contextMenuItems=contextMenuItems .emptyContextMenuItems=emptyContextMenuItems:
      - rtgl-view flex=1 h=f:
          - rtgl-view h=48 bwb=xs bc=bo d=h av=c w=f ph=md:
              - rtgl-button#back-button sq icon="chevronLeft" v="gh" mr=sm:
              - rtgl-view flex=1:
                  - rtgl-text c=mu-fg: ${layout.name}
          - rtgl-view flex=1 sv w=f:
              - 'rtgl-view w=f bgc=mu pos=rel bwb=xs style="margin-left: 1px; aspect-ratio: 16/9;"':
                  - rtgl-view pos=abs cor=full:
                      - 'div#canvas style="width: 100%; height: 100%;display: flex; cursor: ${canvasCursor}"':
              - $if layout.layoutType == 'dialogue':
                  - rtgl-form#dialogue-form w=f h=f .form=dialogueForm .defaultValues=dialogueDefaultValues:
              - $if layout.layoutType == 'choice':
                  - rtgl-form#choice-form w=f h=f .form=choiceForm .defaultValues=choiceDefaultValues .context=choicesContext:
      - rvn-resizable-panel#resizable-detail-panel panel-type=detail-panel w=300 min-w=200 max-w=500 resize-side="left":
          - rtgl-view wh=f slot="content":
              - $if selectedItemId:
                  - rtgl-view h=48 w=f av=c ph=md:
                      - rtgl-text c=mu-fg: ${item.name} (${item.type})
                  - rtgl-view w=f flex=1 sv pv=md:
                      - rvn-layout-edit-panel#layout-edit-panel key=${layoutEditPanelKey} layout-type=${layout.layoutType} item-type=${item.type} .values=item:
                $else:
                  - rtgl-view wh=f av=c ah=c bgc=mu-fg:
                      - rtgl-text s=md c=mu-fg: No selection
