contextMenuItems:
  - label: Container
    type: item
    createType: container
  - label: Sprite
    type: item
    createType: sprite
  # Hidden for now. Restore when layout editor should allow creating
  # spritesheet animations again.
  # - label: Spritesheet Animation
  #   type: item
  #   createType: spritesheet-animation
  # Hidden for now. Restore when layout editor should allow creating
  # particles again.
  # - label: Particle
  #   type: item
  #   createType: particle
  - label: Text
    type: item
    createType: text
  - label: Slider
    type: item
    createType: slider
  - "$when": 'layoutType == "general"'
    label: Fragment
    type: item
    createType: fragment-ref
  - "$when": 'layoutType == "save-load"'
    label: Fragment
    type: item
    createType: fragment-ref
  - "$when": 'layoutType == "confirmDialog"'
    label: Fragment
    type: item
    createType: fragment-ref
  - "$when": 'layoutType == "confirmDialog"'
    label: Container (Confirm OK)
    type: item
    createType: container-confirm-dialog-ok
  - "$when": 'layoutType == "confirmDialog"'
    label: Container (Confirm Cancel)
    type: item
    createType: container-confirm-dialog-cancel
  - "$when": 'layoutType == "dialogue-adv"'
    label: Text (Dialogue Content)
    type: item
    createType: text-dialogue-content
  - "$when": 'layoutType == "dialogue-adv"'
    label: Text (Character Name)
    type: item
    createType: text-character-name
  - "$when": 'layoutType == "dialogue-nvl"'
    label: Container (Dialogue Line)
    type: item
    createType: container-dialogue-line
  - "$when": 'layoutType == "dialogue-nvl"'
    label: Text (Line Character Name)
    type: item
    createType: text-dialogue-line-character-name
  - "$when": 'layoutType == "dialogue-nvl"'
    label: Text (Line Content)
    type: item
    createType: text-dialogue-line-content
  - "$when": 'layoutType == "history"'
    label: Container (History Item)
    type: item
    createType: container-history-line
  - "$when": 'layoutType == "history"'
    label: Text (History Character Name)
    type: item
    createType: text-history-line-character-name
  - "$when": 'layoutType == "history"'
    label: Text (History Line Content)
    type: item
    createType: text-history-line-content
  - "$when": 'layoutType == "choice"'
    label: Container (Choice Item)
    type: item
    createType: container-choice-item
  - "$when": 'layoutType == "save-load"'
    label: Container (Save/Load Slot)
    type: item
    createType: container-save-load-slot
  - "$when": 'layoutType == "save-load"'
    label: Sprite (Save Image)
    type: item
    createType: sprite-save-load-slot-image
  - "$when": 'layoutType == "save-load"'
    label: Text (Save Date)
    type: item
    createType: text-save-load-slot-date
  - "$when": 'layoutType == "choice"'
    label: Text (Choice Item)
    type: item
    createType: text-choice-item-content
  - label: Rename
    type: item
    value: rename-item
  - label: Delete
    type: item
    value: delete-item

emptyContextMenuItems:
  - label: Container
    type: item
    createType: container
  - label: Sprite
    type: item
    createType: sprite
  # Hidden for now. Restore when layout editor should allow creating
  # spritesheet animations again.
  # - label: Spritesheet Animation
  #   type: item
  #   createType: spritesheet-animation
  # Hidden for now. Restore when layout editor should allow creating
  # particles again.
  # - label: Particle
  #   type: item
  #   createType: particle
  - label: Text
    type: item
    createType: text
  - label: Slider
    type: item
    createType: slider
  - "$when": 'layoutType == "general"'
    label: Fragment
    type: item
    createType: fragment-ref
  - "$when": 'layoutType == "save-load"'
    label: Fragment
    type: item
    createType: fragment-ref
  - "$when": 'layoutType == "confirmDialog"'
    label: Fragment
    type: item
    createType: fragment-ref
  - "$when": 'layoutType == "confirmDialog"'
    label: Container (Confirm OK)
    type: item
    createType: container-confirm-dialog-ok
  - "$when": 'layoutType == "confirmDialog"'
    label: Container (Confirm Cancel)
    type: item
    createType: container-confirm-dialog-cancel
  - "$when": 'layoutType == "save-load"'
    label: Container (Save/Load Slot)
    type: item
    createType: container-save-load-slot
  - "$when": 'layoutType == "save-load"'
    label: Sprite (Save Image)
    type: item
    createType: sprite-save-load-slot-image
  - "$when": 'layoutType == "save-load"'
    label: Text (Save Date)
    type: item
    createType: text-save-load-slot-date
  - "$when": 'layoutType == "dialogue-adv"'
    label: Text (Dialogue Content)
    type: item
    createType: text-dialogue-content
  - "$when": 'layoutType == "dialogue-adv"'
    label: Text (Character Name)
    type: item
    createType: text-character-name
  - "$when": 'layoutType == "dialogue-nvl"'
    label: Container (Dialogue Line)
    type: item
    createType: container-dialogue-line
  - "$when": 'layoutType == "dialogue-nvl"'
    label: Text (Line Character Name)
    type: item
    createType: text-dialogue-line-character-name
  - "$when": 'layoutType == "dialogue-nvl"'
    label: Text (Line Content)
    type: item
    createType: text-dialogue-line-content
  - "$when": 'layoutType == "history"'
    label: Container (History Item)
    type: item
    createType: container-history-line
  - "$when": 'layoutType == "history"'
    label: Text (History Character Name)
    type: item
    createType: text-history-line-character-name
  - "$when": 'layoutType == "history"'
    label: Text (History Line Content)
    type: item
    createType: text-history-line-content

controlContextMenuItems:
  - label: Rename
    type: item
    value: rename-item
  - label: Delete
    type: item
    value: delete-item

controlEmptyContextMenuItems:
  - label: Rect
    type: item
    createType: rect
