---
title: Layouts Workflow
description: Covers the layouts page detail, edit dialog, empty search state, and layout editor route.
url: /projects
skipInitialScreenshot: true
viewport:
  id: wide
  width: 1440
  height: 900
steps:
  - action: waitFor
    selector: "[data-testid='create-project-button']"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 250
  - action: select
    testId: create-project-button
    steps:
      - action: click
  - action: waitFor
    selector: rtgl-form#createProjectForm rtgl-input#field0 input
    state: attached
    timeoutMs: 5000
  - action: select
    selector: rtgl-form#createProjectForm rtgl-input#field0 input
    steps:
      - action: clear
      - action: write
        value: VT Layouts Workflow
  - action: select
    selector: rtgl-form#createProjectForm rtgl-input#field1 input
    steps:
      - action: clear
      - action: write
        value: Covers the layouts page.
  - action: wait
    ms: 250
  - action: select
    selector: "rtgl-form#createProjectForm rtgl-button[data-action-id=submit]"
    steps:
      - action: click
  - action: waitFor
    selector: "#projectItem0"
    state: visible
    timeoutMs: 30000
  - action: wait
    ms: 500
  - action: select
    selector: "#projectItem0"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-project
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: keypress
    key: g
  - action: wait
    ms: 120
  - action: keypress
    key: l
  - action: waitFor
    selector: rvn-layouts
    state: attached
    timeoutMs: 10000
  - action: assert
    type: url
    value: /project/layouts
  - action: wait
    ms: 450
  - action: screenshot
  - action: waitFor
    selector: "#groupview #itemRef0x0"
    state: visible
    timeoutMs: 20000
  - action: waitFor
    selector: "#groupview [data-item-id='3JUCKvusYLYq']"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "#groupview [data-item-id='3JUCKvusYLYq']"
    steps:
      - action: click
  - action: waitFor
    selector: "#detailView"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "#detailHeader"
    steps:
      - action: click
  - action: waitFor
    selector: "#editDialog[open]"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "#editDialog"
    state: hidden
    timeoutMs: 10000
  - action: select
    selector: "#tagFilterButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#tagFilterPopover[open]"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#tagFilterPopover rtgl-input#searchInput"
    steps:
      - action: clear
      - action: write
        value: zzzz vt layout
  - action: wait
    ms: 350
  - action: screenshot
  - action: select
    selector: "#tagFilterPopover rtgl-input#searchInput"
    steps:
      - action: clear
  - action: wait
    ms: 250
  - action: select
    selector: "#tagFilterApply"
    steps:
      - action: click
  - action: waitFor
    selector: "#tagFilterPopover"
    state: hidden
    timeoutMs: 5000
  - action: select
    selector: "#groupview [data-item-id='3JUCKvusYLYq']"
    steps:
      - action: dblclick
  - action: waitFor
    selector: rvn-layout-editor
    state: attached
    timeoutMs: 20000
  - action: assert
    type: url
    value: /project/layout-editor
  - action: wait
    ms: 1200
  - action: screenshot
  - action: select
    selector: "#fileExplorer [data-item-id='AtgPkS3X9ZBF'] rtgl-text"
    steps:
      - action: click
  - action: waitFor
    selector: "#resizableDetailPanel #layoutEditPanel rtgl-segmented-control[data-name='direction'] #option0"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: screenshot
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const canvas = deepQuery(document, 'rvn-layout-editor-canvas#layoutEditorCanvas'); const target = canvas?.deps?.graphicsService?.hitTestElementBounds?.({ x: 532, y: 933 })?.flatMap((hit) => hit.path ?? []).find((entry) => entry.id === 'Q9ufAe6Z2hyD'); const canvasElement = canvas?.shadowRoot?.querySelector?.('#canvas canvas'); const canvasBounds = canvasElement?.getBoundingClientRect?.(); if (!target?.bounds || !canvasBounds?.width) return 'missing-target'; const projectX = target.bounds.x + target.bounds.width / 2; const projectY = target.bounds.y + target.bounds.height / 2; const clientX = canvasBounds.left + projectX / 1920 * canvasBounds.width; const clientY = canvasBounds.top + projectY / 1080 * canvasBounds.height; window.__vtLayoutChildPointer = { clientX, clientY }; const pointer = { bubbles: true, composed: true, pointerId: 41, pointerType: 'mouse', isPrimary: true, button: 0, clientX, clientY }; canvasElement.dispatchEvent(new PointerEvent('pointerdown', { ...pointer, buttons: 1 })); canvasElement.dispatchEvent(new PointerEvent('pointerup', { ...pointer, buttons: 0 })); canvasElement.dispatchEvent(new PointerEvent('click', { ...pointer, detail: 1 })); return 'clicked'; })()"
    value: "clicked"
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const canvas = deepQuery(document, 'rvn-layout-editor-canvas#layoutEditorCanvas'); return String(canvas?.props?.selectedItemId === 'AtgPkS3X9ZBF'); })()"
    value: "true"
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const canvas = deepQuery(document, 'rvn-layout-editor-canvas#layoutEditorCanvas'); const canvasElement = canvas?.shadowRoot?.querySelector?.('#canvas canvas'); const position = window.__vtLayoutChildPointer; if (!canvasElement || !position) return 'missing-target'; const pointer = { bubbles: true, composed: true, pointerId: 42, pointerType: 'mouse', isPrimary: true, button: 0, clientX: position.clientX, clientY: position.clientY }; canvasElement.dispatchEvent(new PointerEvent('pointerdown', { ...pointer, buttons: 1 })); canvasElement.dispatchEvent(new PointerEvent('pointerup', { ...pointer, buttons: 0 })); canvasElement.dispatchEvent(new PointerEvent('click', { ...pointer, detail: 2 })); canvasElement.dispatchEvent(new MouseEvent('dblclick', { ...pointer, detail: 2 })); return 'double-clicked'; })()"
    value: "double-clicked"
  - action: wait
    ms: 300
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const findById = (elements, id) => { for (const element of Array.isArray(elements) ? elements : []) { if (element?.id === id) return element; const child = findById(element?.children, id); if (child) return child; } }; const canvas = deepQuery(document, 'rvn-layout-editor-canvas#layoutEditorCanvas'); const elements = canvas?.store?.selectCanvasRenderState?.()?.elements; const overlay = findById(elements, 'selected-border-outer'); const moveSurface = findById(elements, 'selected-border'); return String(canvas?.props?.selectedItemId === 'Q9ufAe6Z2hyD' && overlay?.border?.width > 0 && overlay?.fill === 'transparent' && moveSurface?.drag === undefined); })()"
    value: "true"
  - action: select
    selector: "#fileExplorer [data-item-id='AtgPkS3X9ZBF'] rtgl-text"
    steps:
      - action: click
  - action: wait
    ms: 300
  - action: select
    selector: "#layoutEditPanel [data-name='rotation']"
    steps:
      - action: click
  - action: waitFor
    selector: "#layoutEditPanel #popoverForm[open]"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "#layoutEditPanel #popoverForm rtgl-input-number[data-field-name='value'] input"
    steps:
      - action: clear
      - action: write
        value: "8"
  - action: select
    selector: "#layoutEditPanel #popoverForm rtgl-button[data-action-id='submit']"
    steps:
      - action: click
  - action: wait
    ms: 600
  - action: screenshot
  - action: select
    selector: "#fileExplorer #itemRef1"
    steps:
      - action: rclick
  - action: waitFor
    selector: "#fileExplorer #dropdownMenu[open]"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 250
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "#fileExplorer #dropdownMenu[open]"
    state: hidden
    timeoutMs: 5000
  - action: waitFor
    selector: "#fileExplorer [data-item-id='UK85MSpkHfto']"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "#fileExplorer [data-item-id='UK85MSpkHfto'] rtgl-text"
    steps:
      - action: click
  - action: waitFor
    selector: "#layoutEditPanel rtgl-select[data-name='dateFormat'] [data-testid='select-button']"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "#layoutEditPanel rtgl-select[data-name='dateFormat'] [data-testid='select-button']"
    steps:
      - action: click
  - action: waitFor
    selector: "#layoutEditPanel rtgl-select[data-name='dateFormat'] #option3"
    state: visible
    timeoutMs: 5000
  - action: screenshot
  - action: select
    selector: "#layoutEditPanel rtgl-select[data-name='dateFormat'] #option3"
    steps:
      - action: click
  - action: wait
    ms: 600
  - action: screenshot
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { window.dispatchEvent(new CustomEvent('routevn:vt:navigate', { detail: { path: '/project/layouts' } })); return 'requested'; })()"
    value: "requested"
  - action: waitFor
    selector: rvn-layouts
    state: attached
    timeoutMs: 10000
  - action: assert
    type: url
    value: /project/layouts
  - action: waitFor
    selector: "#groupview [data-item-id='fKr5fa67MQWh']"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#groupview [data-item-id='fKr5fa67MQWh']"
    steps:
      - action: dblclick
  - action: waitFor
    selector: rvn-layout-editor
    state: attached
    timeoutMs: 20000
  - action: waitFor
    selector: "#fileExplorer [data-item-id='wTne3pvWw7ui'] rtgl-text"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#fileExplorer [data-item-id='wTne3pvWw7ui'] rtgl-text"
    steps:
      - action: click
  - action: wait
    ms: 300
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const findById = (elements, id) => { for (const element of Array.isArray(elements) ? elements : []) { if (element?.id === id) return element; const child = findById(element?.children, id); if (child) return child; } }; const fragmentId = 'wTne3pvWw7ui'; const canvas = deepQuery(document, 'rvn-layout-editor-canvas#layoutEditorCanvas'); const renderState = canvas?.store?.selectCanvasRenderState?.(); const occurrenceState = canvas?.store?.selectSelectionOccurrenceState?.(); const root = findById(renderState?.baseElements, fragmentId); const descendantIds = (occurrenceState?.occurrenceIdsByOwner?.[fragmentId] ?? []).filter((id) => id !== fragmentId); const descendants = descendantIds.map((id) => findById(renderState?.baseElements, id)).filter(Boolean); if (!canvas || !root || descendants.length < 2) return 'missing-fragment'; window.__vtFragmentDragBaseline = descendants.map(({ id, x, y, width, height, rotation }) => ({ id, x, y, width, height, rotation })); window.__vtFragmentDragExpected = { x: (root.x ?? 0) + 20, y: (root.y ?? 0) + 10 }; const graphicsService = canvas.deps.graphicsService; window.__vtFragmentOriginalRender = graphicsService.render; graphicsService.render = (payload) => { window.__vtFragmentDragElements = payload?.elements; return window.__vtFragmentOriginalRender(payload); }; canvas.deps.subject.dispatch('border-drag-start', { targetId: 'selected-border' }); canvas.deps.subject.dispatch('border-drag-move', { x: 0, y: 0 }); canvas.deps.subject.dispatch('border-drag-move', { x: 20, y: 10 }); return canvas.store.selectDragging().isDragging && window.__vtFragmentDragElements ? 'dragged' : 'missing-drag'; })()"
    value: "dragged"
  - action: assert
    type: js
    fn: eval
    args:
      - |
        (async () => {
          const deepQuery = (root, selector) => {
            const direct = root?.querySelector?.(selector);
            if (direct) return direct;
            for (const node of root?.querySelectorAll?.('*') ?? []) {
              const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined;
              if (found) return found;
            }
          };
          const findById = (elements, id) => {
            for (const element of elements ?? []) {
              if (element.id === id) return element;
              const child = findById(element.children, id);
              if (child) return child;
            }
          };
          const canvas = deepQuery(document, 'rvn-layout-editor-canvas#layoutEditorCanvas');
          await canvas.transformedHandlers.restartPreview({});
          canvas.deps.subject.dispatch('border-drag-move', { x: 30, y: 15 });
          const moved = findById(window.__vtFragmentDragElements, 'wTne3pvWw7ui');
          const expected = window.__vtFragmentDragExpected;
          const stayedUnderPointer = moved.x === expected.x + 10 && moved.y === expected.y + 5;
          canvas.deps.subject.dispatch('border-drag-move', { x: 20, y: 10 });
          return String(stayedUnderPointer);
        })()
    value: "true"
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const findById = (elements, id) => { for (const element of Array.isArray(elements) ? elements : []) { if (element?.id === id) return element; const child = findById(element?.children, id); if (child) return child; } }; const root = findById(window.__vtFragmentDragElements, 'wTne3pvWw7ui'); const fields = ['x', 'y', 'width', 'height', 'rotation']; const descendantsStayedLocal = window.__vtFragmentDragBaseline.every((before) => { const after = findById(window.__vtFragmentDragElements, before.id); return after && fields.every((field) => Object.is(after[field], before[field])); }); return String(root?.x === window.__vtFragmentDragExpected.x && root?.y === window.__vtFragmentDragExpected.y && descendantsStayedLocal); })()"
    value: "true"
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const canvas = deepQuery(document, 'rvn-layout-editor-canvas#layoutEditorCanvas'); if (!canvas || !window.__vtFragmentOriginalRender) return 'missing-drag'; canvas.deps.graphicsService.render = window.__vtFragmentOriginalRender; canvas.deps.subject.dispatch('border-drag-end'); delete window.__vtFragmentOriginalRender; delete window.__vtFragmentDragElements; delete window.__vtFragmentDragBaseline; return 'released'; })()"
    value: "released"
  - action: wait
    ms: 500
  - action: assert
    type: js
    fn: eval
    args:
      - |
        (() => {
          const deepQuery = (root, selector) => {
            const direct = root?.querySelector?.(selector);
            if (direct) return direct;
            for (const node of root?.querySelectorAll?.('*') ?? []) {
              const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined;
              if (found) return found;
            }
          };
          const findById = (elements, id) => {
            for (const element of elements ?? []) {
              if (element.id === id) return element;
              const child = findById(element.children, id);
              if (child) return child;
            }
          };
          const canvas = deepQuery(document, 'rvn-layout-editor-canvas#layoutEditorCanvas');
          const root = findById(canvas.store.selectCanvasRenderState().baseElements, 'wTne3pvWw7ui');
          const expected = window.__vtFragmentDragExpected;
          delete window.__vtFragmentDragExpected;
          return String(root.x === expected.x && root.y === expected.y);
        })()
    value: "true"
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { window.dispatchEvent(new CustomEvent('routevn:vt:navigate', { detail: { path: '/project/layouts' } })); return 'requested'; })()"
    value: "requested"
  - action: waitFor
    selector: rvn-layouts
    state: attached
    timeoutMs: 10000
  - action: waitFor
    selector: "#groupview [data-item-id='6E1mZaxeXgaN']"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#groupview [data-item-id='6E1mZaxeXgaN']"
    steps:
      - action: dblclick
  - action: waitFor
    selector: rvn-layout-editor
    state: attached
    timeoutMs: 20000
  - action: waitFor
    selector: "#dialogueForm rtgl-segmented-control[data-field-name='dialogue-skip-mode'] #option1"
    state: visible
    timeoutMs: 10000
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const customNameField = deepQuery(document, \"rtgl-segmented-control[data-field-name='dialogue-custom-character-name']\"); const characterNameField = deepQuery(document, \"rtgl-input[data-field-name='dialogue-character-name']\"); const customNameTop = customNameField?.closest('.form-field')?.getBoundingClientRect()?.top; const characterNameTop = characterNameField?.closest('.form-field')?.getBoundingClientRect()?.top; return String(Number.isFinite(customNameTop) && Number.isFinite(characterNameTop) && Math.abs(customNameTop - characterNameTop) < 1); })()"
    value: "true"
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const findById = (elements, id) => { for (const element of Array.isArray(elements) ? elements : []) { if (element?.id === id) return element; const child = findById(element?.children, id); if (child) return child; } }; const canvas = deepQuery(document, 'rvn-layout-editor-canvas#layoutEditorCanvas'); const elements = canvas?.store?.selectCanvasRenderState?.()?.baseElements; return String(canvas?.props?.previewData?.runtime?.skipMode === false && !findById(elements, '5Ubcqe8z5qbv')); })()"
    value: "true"
  - action: select
    selector: "#dialogueForm rtgl-segmented-control[data-field-name='dialogue-skip-mode'] #option1"
    steps:
      - action: click
  - action: assert
    type: js
    fn: eval
    args:
      - "(async () => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const findById = (elements, id) => { for (const element of Array.isArray(elements) ? elements : []) { if (element?.id === id) return element; const child = findById(element?.children, id); if (child) return child; } }; const canvas = deepQuery(document, 'rvn-layout-editor-canvas#layoutEditorCanvas'); const deadline = Date.now() + 10000; while (Date.now() < deadline) { const elements = canvas?.store?.selectCanvasRenderState?.()?.baseElements; if (canvas?.props?.previewData?.runtime?.skipMode === true && Boolean(findById(elements, '5Ubcqe8z5qbv'))) return 'true'; await new Promise((resolve) => setTimeout(resolve, 100)); } return 'false'; })()"
    value: "true"
---
