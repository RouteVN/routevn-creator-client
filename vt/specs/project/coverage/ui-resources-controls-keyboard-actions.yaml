---
title: Controls Keyboard Actions
description: Captures the seeded controls page detail panel, embedded keyboard action summary, and command editor.
url: /projects
skipInitialScreenshot: true
viewport:
  id: wide
  width: 1440
  height: 900
steps:
  - action: waitFor
    selector: "[data-testid='create-project-button']"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 250
  - action: select
    testId: create-project-button
    steps:
      - action: click
  - action: waitFor
    selector: rtgl-global-ui rtgl-form#createProjectForm rtgl-input#field0 input
    state: attached
    timeoutMs: 5000
  - action: select
    selector: rtgl-global-ui rtgl-form#createProjectForm rtgl-input#field0 input
    steps:
      - action: clear
      - action: write
        value: VT Controls Keyboard Actions
  - action: select
    selector: rtgl-global-ui rtgl-form#createProjectForm rtgl-input#field1 input
    steps:
      - action: clear
      - action: write
        value: Captures the seeded controls page detail panel, embedded keyboard action summary, and command editor.
  - action: wait
    ms: 250
  - action: select
    selector: rtgl-global-ui rtgl-button[data-action-id='submit']
    steps:
      - action: click
  - action: waitFor
    selector: "#projectItem0"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: select
    selector: "#projectItem0"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-project
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: select
    selector: "#sidebar [data-item-id='/project/controls']"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-controls
    state: attached
    timeoutMs: 10000
  - action: assert
    type: url
    value: /project/controls
  - action: wait
    ms: 450
  - action: screenshot
  - action: waitFor
    selector: "#groupview #itemRef0x0"
    state: visible
    timeoutMs: 20000
  - action: select
    selector: "#groupview #itemRef0x0"
    steps:
      - action: click
  - action: waitFor
    selector: "#detailView"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "#keyboardItem1"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-system-actions#keyboardSystemActions
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: waitFor
    selector: "#commandLineToggleDialogueUI"
    state: attached
    timeoutMs: 5000
  - action: screenshot
---
