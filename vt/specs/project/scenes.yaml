---
title: Project Scenes Workflow
description: Covers the scenes page, add-scene flow, scene detail, preview, scene editor, background and screen animation playback controls, and sections overview.
url: /projects
skipInitialScreenshot: true
viewport:
  id: wide
  width: 1440
  height: 900
steps:
  - action: waitFor
    selector: "[data-testid='create-project-button']"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 250
  - action: select
    testId: create-project-button
    steps:
      - action: click
  - action: waitFor
    selector: rtgl-form#createProjectForm rtgl-input#field0 input
    state: attached
    timeoutMs: 5000
  - action: select
    selector: rtgl-form#createProjectForm rtgl-input#field0 input
    steps:
      - action: clear
      - action: write
        value: VT Project Scenes Workflow
  - action: select
    selector: rtgl-form#createProjectForm rtgl-input#field1 input
    steps:
      - action: clear
      - action: write
        value: Covers scenes map, previews, and scene editor.
  - action: select
    selector: "rtgl-form#createProjectForm rtgl-select#field2 [data-testid='select-button']"
    steps:
      - action: click
  - action: waitFor
    selector: "rtgl-form#createProjectForm rtgl-select#field2 #option1"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "rtgl-form#createProjectForm rtgl-select#field2 #option1"
    steps:
      - action: click
  - action: wait
    ms: 250
  - action: select
    selector: "#createProjectSubmitButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#projectItem0"
    state: visible
    timeoutMs: 30000
  - action: wait
    ms: 500
  - action: select
    selector: "#projectItem0"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-project
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: select
    selector: "#sidebar [data-item-id='animatedAssets']"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-animations
    state: attached
    timeoutMs: 10000
  - action: waitFor
    selector: "#groupview #addBtnHeaderRef1"
    state: visible
    timeoutMs: 20000
  - action: select
    selector: "#groupview #addBtnHeaderRef1"
    steps:
      - action: click
  - action: waitFor
    selector: "#addDialog[open]"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#addDialog rtgl-input[data-field-name='name'] input"
    steps:
      - action: write
        value: Looping Move
  - action: select
    selector: "#addDialog rtgl-button[data-action-id='submit']"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-animation-editor
    state: attached
    timeoutMs: 20000
  - action: waitFor
    selector: "#addPropertiesButton"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#addPropertiesButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#addPropertyForm"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#addPropertyForm rtgl-button[data-action-id='submit']"
    steps:
      - action: click
  - action: waitFor
    selector: "#updateTimeline #track0"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "#updateTimeline #track0"
    steps:
      - action: click
  - action: waitFor
    selector: "#addKeyframeForm"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#addKeyframeForm rtgl-button[data-action-id='submit']"
    steps:
      - action: click
  - action: waitFor
    selector: "#updateTimeline #keyframe0x0"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "#backButton"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-animations
    state: attached
    timeoutMs: 10000
  - action: select
    selector: "#sidebar [data-item-id='scenes']"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-scenes
    state: attached
    timeoutMs: 10000
  - action: assert
    type: url
    value: /project/scenes
  - action: waitFor
    selector: "#whiteboard #container"
    state: visible
    timeoutMs: 10000
  - action: waitFor
    selector: "#mapAddHintClose"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 800
  - action: screenshot
  - action: select
    selector: "#mapAddHintClose"
    steps:
      - action: click
  - action: assert
    type: hidden
    selector: "#mapAddHintClose"
  - action: select
    selector: "#whiteboard #container"
    steps:
      - action: rclick
  - action: wait
    ms: 250
  - action: waitFor
    selector: "#sceneFormPopover"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#sceneFormPopover rtgl-input[data-field-name='name']"
    steps:
      - action: write
        value: VT Map Added Scene
  - action: screenshot
  - action: select
    selector: "#sceneFormPopover rtgl-button[data-action-id='submit']"
    steps:
      - action: click
  - action: waitFor
    selector: "#sceneFormPopover"
    state: hidden
    timeoutMs: 10000
  - action: wait
    ms: 600
  - action: screenshot
  - action: waitFor
    selector: "#whiteboard #itemRef1"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#whiteboard #itemRef1"
    steps:
      - action: click
  - action: waitFor
    selector: "#detailHeader"
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 350
  - action: screenshot
  - action: select
    selector: "#fileExplorerDetailKeyboardScope"
    steps:
      - action: focus
  - action: keypress
    key: e
  - action: waitFor
    selector: "#editDialog[open]"
    state: attached
    timeoutMs: 5000
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "#editDialog"
    state: hidden
    timeoutMs: 10000
  - action: select
    selector: "#sectionsListToggle"
    steps:
      - action: click
  - action: assert
    type: hidden
    selector: "#sectionsListItem0"
  - action: wait
    ms: 250
  - action: screenshot
  - action: select
    selector: "#sectionsListToggle"
    steps:
      - action: click
  - action: waitFor
    selector: "#sectionsListItem0"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "#detailPreviewButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#vnPreview"
    state: attached
    timeoutMs: 10000
  - action: waitFor
    selector: "#vnPreview [data-preview-ready='true']"
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 150
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "#vnPreview"
    state: hidden
    timeoutMs: 10000
  - action: select
    selector: "#whiteboard #itemRef1"
    steps:
      - action: dblclick
  - action: waitFor
    selector: rvn-scene-editor-lexical
    state: attached
    timeoutMs: 20000
  - action: assert
    type: url
    value: /project/scene-editor
  - action: wait
    ms: 1600
  - action: waitFor
    selector: 'rvn-lexical-scene-document-editor .editor-paragraph[data-selected="true"][data-mode="block"]'
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 1200
  - action: assert
    type: text
    selector: "#sceneTextStats"
    value: "5 lines 0 characters"
  - action: assert
    type: visible
    selector: "#downloadCanvasButton"
  - action: screenshot
  - action: select
    selector: "#systemActions #addActionButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-system-actions-dialog-surface rtgl-dialog[open][bare]"
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 250
  - action: screenshot
  - action: select
    selector: "rvn-command-line-actions#commandLineActions #action4"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-background#commandLineBackground rtgl-select[data-field-name='animationId']"
    state: attached
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-background#commandLineBackground rtgl-select[data-field-name='animationId'] [data-testid='select-button']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-background#commandLineBackground rtgl-select[data-field-name='animationId'] #option2"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-background#commandLineBackground rtgl-select[data-field-name='animationId'] #option2"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-background#commandLineBackground rtgl-segmented-control[data-field-name='playbackLoop']"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-background#commandLineBackground rtgl-select[data-field-name='animationId'] [data-testid='select-button']"
    steps:
      - action: hover
      - action: scroll
        deltaX: 0
        deltaY: 500
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const background = deepQuery(document, 'rvn-command-line-background#commandLineBackground'); const form = deepQuery(background.shadowRoot, 'rtgl-form#form'); const fields = [...form.shadowRoot.querySelectorAll('[data-field-name]')].map((field) => field.dataset.fieldName); const loop = deepQuery(form.shadowRoot, 'rtgl-segmented-control[data-field-name=playbackLoop]'); return `${fields.slice(-3).join(',')}:${loop.hasAttribute('disabled')}`; })()"
    value: "playbackSpeed,playbackLoop,playbackContinuity:true"
  - action: wait
    ms: 250
  - action: screenshot
  - action: select
    selector: "rvn-command-line-background#commandLineBackground rtgl-select[data-field-name='animationId'] [data-testid='select-button']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-background#commandLineBackground rtgl-select[data-field-name='animationId'] #option3"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-background#commandLineBackground rtgl-select[data-field-name='animationId'] #option3"
    steps:
      - action: click
  - action: select
    selector: "rvn-command-line-background#commandLineBackground rtgl-select[data-field-name='animationId'] [data-testid='select-button']"
    steps:
      - action: hover
      - action: scroll
        deltaX: 0
        deltaY: 500
  - action: waitFor
    selector: "rvn-command-line-background#commandLineBackground rtgl-input-number[data-field-name='playbackSpeed'] input"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-background#commandLineBackground rtgl-input-number[data-field-name='playbackSpeed'] input"
    steps:
      - action: clear
      - action: write
        value: "1.5"
  - action: select
    selector: "rvn-command-line-background#commandLineBackground rtgl-segmented-control[data-field-name='playbackLoop'] #option1"
    steps:
      - action: click
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const background = deepQuery(document, 'rvn-command-line-background#commandLineBackground'); const form = deepQuery(background.shadowRoot, 'rtgl-form#form'); const speedField = deepQuery(form.shadowRoot, 'rtgl-input-number[data-field-name=playbackSpeed]'); const speed = deepQuery(speedField.shadowRoot, 'input'); const loop = deepQuery(form.shadowRoot, 'rtgl-segmented-control[data-field-name=playbackLoop]'); const loopOption = deepQuery(loop.shadowRoot, '#option1'); return `${speed.value}:${loop.hasAttribute('disabled')}:${loopOption.getAttribute('aria-pressed')}`; })()"
    value: "1.5:false:true"
  - action: wait
    ms: 250
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "rvn-system-actions-dialog-surface rtgl-dialog[open]"
    state: hidden
    timeoutMs: 10000
  - action: select
    selector: "#systemActions #addActionButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-actions#commandLineActions #action3"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-actions#commandLineActions #action3"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-system-actions#systemActions rvn-command-line-screen#commandLineScreen rtgl-select[data-field-name='transitionAnimationId']"
    state: attached
    timeoutMs: 10000
  - action: select
    selector: "rvn-system-actions#systemActions rvn-command-line-screen#commandLineScreen rtgl-select[data-field-name='transitionAnimationId'] [data-testid='select-button']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-system-actions#systemActions rvn-command-line-screen#commandLineScreen rtgl-select[data-field-name='transitionAnimationId'] #option0"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-system-actions#systemActions rvn-command-line-screen#commandLineScreen rtgl-select[data-field-name='transitionAnimationId'] #option0"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-system-actions#systemActions rvn-command-line-screen#commandLineScreen rtgl-input-number[data-field-name='playbackSpeed'] input"
    state: visible
    timeoutMs: 5000
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const systemActions = deepQuery(document, 'rvn-system-actions#systemActions'); const screen = deepQuery(systemActions.shadowRoot, 'rvn-command-line-screen#commandLineScreen'); const form = deepQuery(screen.shadowRoot, 'rtgl-form#form'); const speedField = deepQuery(form.shadowRoot, 'rtgl-input-number[data-field-name=playbackSpeed]'); const speed = deepQuery(speedField.shadowRoot, 'input'); const continuity = deepQuery(form.shadowRoot, 'rtgl-segmented-control[data-field-name=playbackContinuity]'); const loop = deepQuery(form.shadowRoot, '[data-field-name=playbackLoop]'); return `${speed.value}:${Boolean(continuity)}:${Boolean(loop)}`; })()"
    value: "1:true:false"
  - action: wait
    ms: 250
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "rvn-system-actions-dialog-surface rtgl-dialog[open]"
    state: hidden
    timeoutMs: 10000
  - action: select
    selector: "#systemActions #addActionButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-actions#commandLineActions #action12"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-actions#commandLineActions #action12"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-choices#commandLineChoices #addChoiceButton"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-choices#commandLineChoices #addChoiceButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-choices#commandLineChoices rtgl-segmented-control[data-field-name='actionType'] #option1"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-choices#commandLineChoices rtgl-segmented-control[data-field-name='actionType'] #option1"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-choices#commandLineChoices rtgl-select[data-field-name='transitionAnimationId'] [data-testid='select-button']"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-choices#commandLineChoices rtgl-select[data-field-name='transitionAnimationId'] [data-testid='select-button']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-choices#commandLineChoices rtgl-select[data-field-name='transitionAnimationId'] #option0"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-choices#commandLineChoices rtgl-select[data-field-name='transitionAnimationId'] #option0"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-choices#commandLineChoices rtgl-input-number[data-field-name='playbackSpeed'] input"
    state: visible
    timeoutMs: 5000
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const systemActions = deepQuery(document, 'rvn-system-actions#systemActions'); const choices = deepQuery(systemActions.shadowRoot, 'rvn-command-line-choices#commandLineChoices'); const form = deepQuery(choices.shadowRoot, 'rtgl-form#choiceForm'); const speedField = deepQuery(form.shadowRoot, 'rtgl-input-number[data-field-name=playbackSpeed]'); const speed = deepQuery(speedField.shadowRoot, 'input'); const continuity = deepQuery(form.shadowRoot, 'rtgl-segmented-control[data-field-name=playbackContinuity]'); const continuityOption = deepQuery(continuity.shadowRoot, '#option0'); return `${speed.value}:${continuityOption.getAttribute('aria-pressed')}`; })()"
    value: "1:true"
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "rvn-system-actions-dialog-surface rtgl-dialog[open]"
    state: hidden
    timeoutMs: 10000
  - action: select
    selector: "#sectionsOverview"
    steps:
      - action: click
  - action: waitFor
    selector: "#sectionsOverviewDialog"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 400
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "#sectionsOverviewDialog"
    state: hidden
    timeoutMs: 10000
  - action: select
    selector: "#previewButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#vnPreview"
    state: attached
    timeoutMs: 10000
  - action: waitFor
    selector: "#vnPreview [data-preview-ready='true']"
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 150
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "#vnPreview"
    state: hidden
    timeoutMs: 10000
  - action: select
    selector: "#sidebar [data-item-id='scenes']"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-scenes
    state: attached
    timeoutMs: 10000
  - action: waitFor
    selector: "#whiteboard #itemRef1"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#whiteboard #itemRef1"
    steps:
      - action: click
  - action: assert
    type: hidden
    selector: "#selectedSceneTextStats"
  - action: screenshot
---
