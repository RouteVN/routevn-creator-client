---
title: Sounds Workflow
description: Covers the sounds page upload, detail, keyboard audio preview, edit submit, and filtered search states.
url: /projects
skipInitialScreenshot: true
viewport:
  id: wide
  width: 1440
  height: 900
steps:
  - action: waitFor
    selector: "[data-testid='create-project-button']"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 250
  - action: select
    testId: create-project-button
    steps:
      - action: click
  - action: waitFor
    selector: rtgl-form#createProjectForm rtgl-input#field0 input
    state: attached
    timeoutMs: 5000
  - action: select
    selector: rtgl-form#createProjectForm rtgl-input#field0 input
    steps:
      - action: clear
      - action: write
        value: VT Sounds Workflow
  - action: select
    selector: rtgl-form#createProjectForm rtgl-input#field1 input
    steps:
      - action: clear
      - action: write
        value: Covers the sounds page.
  - action: wait
    ms: 250
  - action: select
    selector: "#createProjectSubmitButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#projectItem0"
    state: visible
    timeoutMs: 30000
  - action: wait
    ms: 500
  - action: select
    selector: "#projectItem0"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-project
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: customEvent
    name: routevn:vt:navigate
    detail:
      path: /project/sounds
  - action: waitFor
    selector: rvn-sounds
    state: attached
    timeoutMs: 10000
  - action: assert
    type: url
    value: /project/sounds
  - action: wait
    ms: 450
  - action: screenshot
  - action: waitFor
    selector: "#uploadBtnHeaderRef0"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#uploadBtnHeaderRef0"
    steps:
      - action: click
  - action: waitFor
    selector: "#rtglVtFilePickerInput"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#rtglVtFilePickerInput"
    steps:
      - action: upload
        files:
          - vt/fixtures/sine.wav
  - action: wait
    ms: 500
  - action: waitFor
    selector: "#groupview #itemRef0x0[data-item-id]:not([data-item-id=''])"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#groupview #itemRef0x0[data-item-id]:not([data-item-id=''])"
    steps:
      - action: click
  - action: waitFor
    selector: "#detailView"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 400
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { if (!root) return undefined; const direct = root.querySelector?.(selector); if (direct) return direct; for (const node of root.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const preview = deepQuery(document, '#detailSoundPreview'); return preview ? getComputedStyle(preview).pointerEvents : undefined; })()"
    value: none
  - action: screenshot
  - action: keypress
    key: Enter
  - action: waitFor
    selector: "#rvnAudioPlayer"
    state: attached
    timeoutMs: 10000
  - action: waitFor
    selector: "#rvnAudioPlayer #playPauseBtn rtgl-svg[svg='pause']"
    state: attached
    timeoutMs: 5000
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { if (!root) return undefined; const direct = root.querySelector?.(selector); if (direct) return direct; for (const node of root.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const player = deepQuery(document, '#rvnAudioPlayer'); const playerBody = deepQuery(player?.shadowRoot, 'rtgl-view[bgc=\"mu\"]'); const playButton = deepQuery(player?.shadowRoot, '#playPauseBtn'); if (!playerBody || !playButton) return false; const playerRect = playerBody.getBoundingClientRect(); const buttonRect = playButton.getBoundingClientRect(); return Math.abs(buttonRect.left + buttonRect.width / 2 - (playerRect.left + playerRect.width / 2)) <= 1; })()"
    value: true
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { if (!root) return undefined; const direct = root.querySelector?.(selector); if (direct) return direct; for (const node of root.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const player = deepQuery(document, '#rvnAudioPlayer'); const progressTrack = deepQuery(player?.shadowRoot, '#progressBar'); const playerBody = deepQuery(player?.shadowRoot, 'rtgl-view[bgc=\"mu\"]'); return Boolean(progressTrack && playerBody && getComputedStyle(progressTrack).backgroundColor !== getComputedStyle(playerBody).backgroundColor); })()"
    value: true
  - action: keypress
    key: Enter
  - action: waitFor
    selector: "#rvnAudioPlayer #playPauseBtn rtgl-svg[svg='play']"
    state: attached
    timeoutMs: 5000
  - action: keypress
    key: Enter
  - action: waitFor
    selector: "#rvnAudioPlayer #playPauseBtn rtgl-svg[svg='pause']"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 700
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "#rvnAudioPlayer"
    state: hidden
    timeoutMs: 5000
  - action: select
    selector: "#fileExplorerKeyboardScope"
    steps:
      - action: focus
  - action: keypress
    key: e
  - action: waitFor
    selector: "#editDialog[open]"
    state: attached
    timeoutMs: 5000
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "#editDialog"
    state: hidden
    timeoutMs: 5000
  - action: select
    selector: "#fileExplorerKeyboardScope"
    steps:
      - action: focus
  - action: keypress
    key: j
  - action: keypress
    key: k
  - action: keypress
    key: e
  - action: waitFor
    selector: "#editDialog[open]"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: select
    selector: "#editDialog rtgl-input#field0 input"
    steps:
      - action: clear
      - action: write
        value: Searchable Sine
  - action: screenshot
  - action: select
    selector: "#editDialog rtgl-button[data-action-id='submit']"
    steps:
      - action: click
  - action: waitFor
    selector: "#editDialog"
    state: hidden
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: screenshot
  - action: select
    selector: "#tagFilterButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#tagFilterPopover[open]"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#tagFilterPopover rtgl-input#searchInput"
    steps:
      - action: clear
      - action: write
        value: Searchable Sine
  - action: wait
    ms: 350
  - action: screenshot
  - action: keypress
    key: Escape
  - action: customEvent
    name: routevn:vt:navigate
    detail:
      path: /project/scenes
  - action: waitFor
    selector: rvn-scenes
    state: attached
    timeoutMs: 10000
  - action: waitFor
    selector: "#whiteboard #itemRef0"
    state: visible
    timeoutMs: 10000
  - action: waitFor
    selector: "#mapAddHintClose"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "#mapAddHintClose"
    steps:
      - action: click
  - action: select
    selector: "#whiteboard #itemRef0"
    steps:
      - action: dblclick
  - action: waitFor
    selector: rvn-scene-editor-lexical
    state: attached
    timeoutMs: 20000
  - action: wait
    ms: 1200
  - action: select
    selector: "rvn-system-actions#systemActions #addActionButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-actions#commandLineActions [data-action-id='5']"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-actions#commandLineActions [data-action-id='5']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-bgm#commandLineBgm #bgmChannel"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 300
  - action: select
    selector: "rvn-command-line-bgm#commandLineBgm #bgmChannel"
    steps:
      - action: hover
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const bgm = deepQuery(document, 'rvn-command-line-bgm#commandLineBgm'); const channel = deepQuery(bgm?.shadowRoot, '#bgmChannel'); const emptyState = channel?.querySelector('rtgl-view[h=\"126\"]'); if (!channel || !emptyState) return false; const style = getComputedStyle(channel); return channel.matches(':hover') && channel.getAttribute('bgc') === 'bg' && channel.getAttribute('h-bgc') === 'mu' && emptyState.getAttribute('bgc') === null && style.backgroundColor === style.getPropertyValue('--muted').trim() && style.borderTopWidth !== '0px' && style.borderColor === style.getPropertyValue('--primary').trim(); })()"
    value: true
  - action: screenshot
  - action: select
    selector: "rvn-command-line-bgm#commandLineBgm #bgmChannel"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-bgm#commandLineBgm #channelEditorDialog[open]"
    state: attached
    timeoutMs: 5000
  - action: waitFor
    selector: "rvn-command-line-bgm#commandLineBgm #emptyAddButton"
    state: visible
    timeoutMs: 5000
  - action: screenshot
  - action: select
    selector: "rvn-command-line-bgm#commandLineBgm #emptyAddButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-bgm#commandLineBgm #resourceItem0x0"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-bgm#commandLineBgm #resourceItem0x0"
    steps:
      - action: click
  - action: select
    selector: "rvn-command-line-bgm#commandLineBgm #buttonSelect"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-bgm#commandLineBgm #soundClip0"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "rvn-command-line-bgm#commandLineBgm rtgl-form#soundForm rtgl-segmented-control[data-field-name='loop'] #option0"
    steps:
      - action: click
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const bgm = deepQuery(document, 'rvn-command-line-bgm#commandLineBgm'); const loop = deepQuery(bgm?.shadowRoot, \"rtgl-segmented-control[data-field-name='loop']\"); const option = deepQuery(loop?.shadowRoot, '#option0'); return option?.getAttribute('aria-pressed'); })()"
    value: "true"
  - action: select
    selector: "rvn-command-line-bgm#commandLineBgm #insertAfterButton0"
    steps:
      - action: hover
  - action: wait
    ms: 250
  - action: screenshot
  - action: select
    selector: "rvn-command-line-bgm#commandLineBgm #insertAfterButton0"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-bgm#commandLineBgm #resourceItem0x0"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-bgm#commandLineBgm #resourceItem0x0"
    steps:
      - action: click
  - action: select
    selector: "rvn-command-line-bgm#commandLineBgm #buttonSelect"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-bgm#commandLineBgm #soundClip1"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 300
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "rvn-command-line-bgm#commandLineBgm #channelEditorDialog[open]"
    state: hidden
    timeoutMs: 5000
  - action: waitFor
    selector: "rvn-command-line-bgm#commandLineBgm #channelPreviewSound1"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 250
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const bgm = deepQuery(document, 'rvn-command-line-bgm#commandLineBgm'); return Boolean(deepQuery(bgm?.shadowRoot, \"rtgl-form#channelForm [data-field-name='loop']\")); })()"
    value: false
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "rvn-command-line-bgm#commandLineBgm"
    state: hidden
    timeoutMs: 5000
  - action: select
    selector: "rvn-system-actions#systemActions #addActionButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-actions#commandLineActions [data-action-id='30']"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-actions#commandLineActions [data-action-id='30']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-voice#commandLineVoice #voiceChannel"
    state: visible
    timeoutMs: 10000
  - action: screenshot
  - action: select
    selector: "rvn-command-line-voice#commandLineVoice #voiceChannel"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-voice#commandLineVoice #channelEditorDialog[open]"
    state: attached
    timeoutMs: 5000
  - action: waitFor
    selector: "rvn-command-line-voice#commandLineVoice #emptyAddButton"
    state: visible
    timeoutMs: 5000
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "rvn-command-line-voice#commandLineVoice #channelEditorDialog[open]"
    state: hidden
    timeoutMs: 5000
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "rvn-command-line-voice#commandLineVoice"
    state: hidden
    timeoutMs: 5000
  - action: select
    selector: "rvn-system-actions#systemActions #addActionButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-actions#commandLineActions [data-action-id='6']"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-actions#commandLineActions [data-action-id='6']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-sound-effects#commandLineSoundEffects #addChannelButton"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-sound-effects#commandLineSoundEffects #addChannelButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-sound-effects#commandLineSoundEffects rtgl-form#addChannelForm rtgl-input#field0 input"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-sound-effects#commandLineSoundEffects rtgl-form#addChannelForm rtgl-input#field0 input"
    steps:
      - action: write
        value: Channel One
  - action: select
    selector: "rvn-command-line-sound-effects#commandLineSoundEffects rtgl-form#addChannelForm rtgl-button[data-action-id='submit']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-sound-effects#commandLineSoundEffects #sfxChannel0"
    state: visible
    timeoutMs: 5000
  - action: screenshot
  - action: select
    selector: "rvn-command-line-sound-effects#commandLineSoundEffects #sfxChannel0"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-sound-effects#commandLineSoundEffects #channelEditorDialog[open]"
    state: attached
    timeoutMs: 5000
  - action: waitFor
    selector: "rvn-command-line-sound-effects#commandLineSoundEffects #emptyAddButton0"
    state: visible
    timeoutMs: 5000
  - action: screenshot
---
