---
title: Project Resource Details
description: Creates a project and captures seeded resource detail panels for colors, fonts, and text styles.
url: /projects
skipInitialScreenshot: true
viewport:
  id: wide
  width: 1440
  height: 900
steps:
  - action: waitFor
    selector: "[data-testid='create-project-button']"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: select
    testId: create-project-button
    steps:
      - action: click
  - action: waitFor
    selector: "rtgl-global-ui rtgl-form#createProjectForm rtgl-input#field0 input"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "rtgl-global-ui rtgl-form#createProjectForm rtgl-input#field0 input"
    steps:
      - action: write
        value: VT Resource Details
  - action: select
    selector: "rtgl-global-ui rtgl-form#createProjectForm rtgl-input#field1 input"
    steps:
      - action: write
        value: Capture seeded resource detail panels.
  - action: wait
    ms: 250
  - action: select
    selector: "rtgl-global-ui rtgl-button[data-action-id='submit']"
    steps:
      - action: click
  - action: waitFor
    selector: "#projectItem0"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: select
    selector: "#projectItem0"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-project"
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: select
    selector: "#sidebar [data-item-id='/project/colors']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-colors"
    state: attached
    timeoutMs: 10000
  - action: waitFor
    selector: "#groupview #itemRef0x0"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#groupview #itemRef0x0"
    steps:
      - action: click
  - action: waitFor
    selector: "#detailView"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "rvn-resource-types [data-item-id='fonts']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-fonts"
    state: attached
    timeoutMs: 10000
  - action: waitFor
    selector: "#groupview #itemRef0x0"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#groupview #itemRef0x0"
    steps:
      - action: click
  - action: waitFor
    selector: "#detailView"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "rvn-resource-types [data-item-id='textStyles']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-text-styles"
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 800
  - action: waitFor
    selector: "#typographyView #itemRef0x0"
    state: visible
    timeoutMs: 20000
  - action: select
    selector: "#typographyView #itemRef0x0"
    steps:
      - action: click
  - action: waitFor
    selector: "#detailView"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: screenshot
---
