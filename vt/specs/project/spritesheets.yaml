---
title: Spritesheets Workflow
description: Covers spritesheet creation, browsing, and use in scene backgrounds and visuals, including visual animation playback controls.
url: /projects
skipInitialScreenshot: true
viewport:
  id: wide
  width: 1440
  height: 900
steps:
  - action: waitFor
    selector: "[data-testid='create-project-button']"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 250
  - action: select
    testId: create-project-button
    steps:
      - action: click
  - action: waitFor
    selector: rtgl-form#createProjectForm rtgl-input#field0 input
    state: attached
    timeoutMs: 5000
  - action: select
    selector: rtgl-form#createProjectForm rtgl-input#field0 input
    steps:
      - action: clear
      - action: write
        value: VT Spritesheets Workflow
  - action: select
    selector: rtgl-form#createProjectForm rtgl-input#field1 input
    steps:
      - action: clear
      - action: write
        value: Covers the spritesheets page.
  - action: wait
    ms: 250
  - action: select
    selector: "rtgl-form#createProjectForm rtgl-button[data-action-id=submit]"
    steps:
      - action: click
  - action: waitFor
    selector: "#projectItem0"
    state: visible
    timeoutMs: 30000
  - action: wait
    ms: 500
  - action: select
    selector: "#projectItem0"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-project
    state: attached
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: customEvent
    name: routevn:vt:navigate
    detail:
      path: /project/spritesheets
  - action: waitFor
    selector: rvn-spritesheets
    state: attached
    timeoutMs: 10000
  - action: assert
    type: url
    value: /project/spritesheets
  - action: wait
    ms: 450
  - action: screenshot
  - action: waitFor
    selector: "#uploadBtnHeaderRef0"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "#uploadBtnHeaderRef0"
    steps:
      - action: click
  - action: waitFor
    selector: "#spritesheetDialog"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "#dialogImageSourceButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#rtglVtFilePickerInput"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#rtglVtFilePickerInput"
    steps:
      - action: upload
        files:
          - vt/fixtures/spritesheet.png
  - action: wait
    ms: 300
  - action: select
    selector: "#dialogAtlasSourceButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#rtglVtFilePickerInput"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#rtglVtFilePickerInput"
    steps:
      - action: upload
        files:
          - vt/fixtures/spritesheet.atlas.json
  - action: wait
    ms: 500
  - action: waitFor
    selector: "#spritesheetDialog rtgl-input#field0 input"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#spritesheetDialog rtgl-input#field0 input"
    steps:
      - action: clear
      - action: write
        value: Pulse Sheet
  - action: waitFor
    selector: "#dialogClipButton0"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "#spritesheetDialog #dialogSubmitButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#spritesheetDialog"
    state: hidden
    timeoutMs: 10000
  - action: waitFor
    selector: "#groupview #itemRef0x0"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: select
    selector: "#groupview #itemRef0x0"
    steps:
      - action: click
  - action: waitFor
    selector: "#detailView"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "#groupview #itemRef0x0"
    steps:
      - action: dblclick
  - action: waitFor
    selector: "#spritesheetDialog"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "#spritesheetDialog"
    state: hidden
    timeoutMs: 10000
  - action: select
    selector: "#tagFilterButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#tagFilterPopover[open]"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#tagFilterPopover rtgl-input#searchInput"
    steps:
      - action: clear
      - action: write
        value: Pulse Sheet
  - action: wait
    ms: 350
  - action: screenshot
  - action: select
    selector: "#tagFilterPopover rtgl-input#searchInput"
    steps:
      - action: clear
      - action: write
        value: zzzz vt spritesheet
  - action: wait
    ms: 350
  - action: screenshot
  - action: keypress
    key: Escape
  - action: waitFor
    selector: "#tagFilterPopover"
    state: hidden
    timeoutMs: 5000
  - action: select
    selector: "#sidebar [data-item-id='scenes']"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-scenes
    state: attached
    timeoutMs: 10000
  - action: waitFor
    selector: "#whiteboard #itemRef0"
    state: visible
    timeoutMs: 10000
  - action: waitFor
    selector: "#mapAddHintClose"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "#mapAddHintClose"
    steps:
      - action: click
  - action: select
    selector: "#whiteboard #itemRef0"
    steps:
      - action: dblclick
  - action: waitFor
    selector: rvn-scene-editor-lexical
    state: attached
    timeoutMs: 20000
  - action: wait
    ms: 1200
  - action: select
    selector: "rvn-system-actions#systemActions #addActionButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-actions#commandLineActions #action4"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-actions#commandLineActions #action4"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-background#commandLineBackground #backgroundImage"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-background#commandLineBackground #backgroundImage"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-background#commandLineBackground #tabs #tab1"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-background#commandLineBackground #tabs #tab1"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-background#commandLineBackground #spritesheetItemRef0x0"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "rvn-command-line-background#commandLineBackground #spritesheetItemRef0x0"
    steps:
      - action: click
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "rvn-command-line-background#commandLineBackground #buttonSelect"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-background#commandLineBackground #backgroundImage"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "rvn-command-line-background#commandLineBackground #submitButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-system-actions#systemActions #actionItemBackground"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "rvn-system-actions#systemActions #addActionButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-actions#commandLineActions #action6"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-actions#commandLineActions #action6"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-visual#commandLineVisual #addVisualButton"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual #addVisualButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-visual#commandLineVisual #addVisualPopover"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual #addVisualPopover rtgl-button[data-action-id='submit']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-visual#commandLineVisual #tabs #tab1"
    state: visible
    timeoutMs: 10000
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual #tabs #tab1"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-visual#commandLineVisual #spritesheetItemRef0x0"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual #spritesheetItemRef0x0"
    steps:
      - action: click
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual #buttonSelect"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-visual#commandLineVisual #visualItem0x0"
    state: visible
    timeoutMs: 10000
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { if (!root) return undefined; const direct = root.querySelector?.(selector); if (direct) return direct; for (const node of root.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const visual = deepQuery(document, 'rvn-command-line-visual#commandLineVisual'); if (!visual) return 'missing'; window.__vtVisualShaderPreview = undefined; visual.addEventListener('temporary-presentation-state-change', (event) => { window.__vtVisualShaderPreview = event.detail?.presentationState?.visual?.items?.[0]?.filters; }); return 'ready'; })()"
    value: ready
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual rtgl-form#form rtgl-button[data-section-id='visual-0'][data-section-action-id='add']"
    steps:
      - action: click
  - action: waitFor
    selector: 'rtgl-global-ui rtgl-dropdown-menu#dropdownMenu [role="menuitem"]:has-text("Brightness")'
    state: visible
    timeoutMs: 5000
  - action: select
    selector: 'rtgl-global-ui rtgl-dropdown-menu#dropdownMenu [role="menuitem"]:has-text("Brightness")'
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-visual#commandLineVisual rtgl-slider-input[data-adjustment-id='brightness'] input[type='number']"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual rtgl-slider-input[data-adjustment-id='brightness'] input[type='number']"
    steps:
      - action: clear
      - action: write
        value: "0.25"
  - action: keypress
    key: Tab
  - action: wait
    ms: 300
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const filter = window.__vtVisualShaderPreview?.find((item) => item.id === 'backgroundBrightness'); return `${filter?.parameters?.brightness}:${filter?.type}:${Boolean(filter?.source?.webgl?.fragment)}:${Boolean(filter?.source?.webgpu?.source)}`; })()"
    value: "0.25:shader:true:true"
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual rtgl-form#form rtgl-button[data-section-id='visual-0-brightness'][data-section-action-id='remove']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-visual#commandLineVisual rtgl-slider-input[data-adjustment-id='brightness']"
    state: hidden
    timeoutMs: 5000
  - action: assert
    type: js
    fn: eval
    args:
      - "String(window.__vtVisualShaderPreview?.some((item) => item.id === 'backgroundBrightness') ?? false)"
    value: "false"
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual #animation0x0 [data-testid='select-button']"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-visual#commandLineVisual #animation0x0 #option3"
    state: visible
    timeoutMs: 5000
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual #animation0x0 #option3"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-command-line-visual#commandLineVisual #animationPlaybackSpeed0x0 input"
    state: visible
    timeoutMs: 5000
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const visual = deepQuery(document, 'rvn-command-line-visual#commandLineVisual'); const speedField = deepQuery(visual.shadowRoot, '#animationPlaybackSpeed0x0'); const speed = deepQuery(speedField.shadowRoot, 'input'); const loop = deepQuery(visual.shadowRoot, '#animationPlaybackLoop0x0'); const continuity = deepQuery(visual.shadowRoot, '#animationPlaybackContinuity0x0'); return `${speed.value}:${loop.disabled}:${Boolean(continuity)}`; })()"
    value: "1:true:true"
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual #animationPlaybackSpeed0x0 input"
    steps:
      - action: clear
      - action: write
        value: "0.5"
  - action: keypress
    key: Tab
  - action: assert
    type: js
    fn: eval
    args:
      - "(() => { const deepQuery = (root, selector) => { const direct = root?.querySelector?.(selector); if (direct) return direct; for (const node of root?.querySelectorAll?.('*') ?? []) { const found = node.shadowRoot ? deepQuery(node.shadowRoot, selector) : undefined; if (found) return found; } return undefined; }; const visual = deepQuery(document, 'rvn-command-line-visual#commandLineVisual'); const speedField = deepQuery(visual.shadowRoot, '#animationPlaybackSpeed0x0'); return deepQuery(speedField.shadowRoot, 'input').value; })()"
    value: "0.5"
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual #animationPlaybackSpeed0x0 input"
    steps:
      - action: clear
      - action: write
        value: "1"
  - action: keypress
    key: Tab
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "rvn-command-line-visual#commandLineVisual #submitButton"
    steps:
      - action: click
  - action: waitFor
    selector: "rvn-system-actions#systemActions #actionItemVisual"
    state: visible
    timeoutMs: 10000
  - action: wait
    ms: 300
  - action: screenshot
---
