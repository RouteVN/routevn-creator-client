---
title: Project Overview Workflow
description: Covers the project overview and edit submit flow on the main project page.
url: /projects
skipInitialScreenshot: true
viewport:
  id: wide
  width: 1440
  height: 900
steps:
  - action: waitFor
    selector: "[data-testid='create-project-button']"
    state: visible
    timeoutMs: 5000
  - action: wait
    ms: 250
  - action: select
    testId: create-project-button
    steps:
      - action: click
  - action: waitFor
    selector: rtgl-form#createProjectForm rtgl-input#field0 input
    state: attached
    timeoutMs: 5000
  - action: select
    selector: rtgl-form#createProjectForm rtgl-input#field0 input
    steps:
      - action: clear
      - action: write
        value: VT Project Overview Workflow
  - action: select
    selector: rtgl-form#createProjectForm rtgl-input#field1 input
    steps:
      - action: clear
      - action: write
        value: Covers project overview detail and edit submit.
  - action: select
    selector: "rtgl-form#createProjectForm rtgl-select#field2 [data-testid='select-button']"
    steps:
      - action: click
  - action: waitFor
    selector: "rtgl-form#createProjectForm rtgl-select#field2 #option1"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "rtgl-form#createProjectForm rtgl-select#field2 #option1"
    steps:
      - action: click
  - action: wait
    ms: 250
  - action: select
    selector: "#createProjectSubmitButton"
    steps:
      - action: click
  - action: waitFor
    selector: "#projectItem0"
    state: visible
    timeoutMs: 30000
  - action: wait
    ms: 500
  - action: select
    selector: "#projectItem0"
    steps:
      - action: click
  - action: waitFor
    selector: rvn-project
    state: attached
    timeoutMs: 10000
  - action: assert
    type: url
    value: /project
  - action: wait
    ms: 500
  - action: screenshot
  - action: select
    selector: "#projectLanguage"
    steps:
      - action: click
  - action: waitFor
    selector: "#editDialog[open]"
    state: attached
    timeoutMs: 5000
  - action: wait
    ms: 300
  - action: screenshot
  - action: select
    selector: "#editDialog rtgl-input#field0 input"
    steps:
      - action: clear
      - action: write
        value: VT Project Edited
  - action: select
    selector: "#editDialog rtgl-textarea#field1 textarea"
    steps:
      - action: clear
      - action: write
        value: Updated by the page-based VT workflow.
  - action: select
    selector: "#editDialog rtgl-select#field2 [data-testid='select-button']"
    steps:
      - action: click
  - action: waitFor
    selector: "#editDialog rtgl-select#field2 #option2"
    state: attached
    timeoutMs: 5000
  - action: select
    selector: "#editDialog rtgl-select#field2 #option2"
    steps:
      - action: click
  - action: screenshot
  - action: select
    selector: "#editDialog rtgl-button[data-action-id='submit']"
    steps:
      - action: click
  - action: waitFor
    selector: "#editDialog"
    state: hidden
    timeoutMs: 10000
  - action: wait
    ms: 500
  - action: screenshot
---
