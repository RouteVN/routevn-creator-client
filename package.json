{
  "type": "module",
  "dependencies": {
    "@rettangoli/fe": "^0.0.7-rc11",
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/plugin-fs": "^2.4.2",
    "@tauri-apps/plugin-sql": "~2",
    "esbuild": "^0.25.5",
    "immer": "^10.1.1",
    "jempl": "0.2.0-rc1",
    "js-yaml": "^4.1.0",
    "jsdom": "^26.1.0",
    "nanoid": "^5.1.5",
    "route-engine-js": "0.0.2-rc16",
    "route-graphics": "0.0.2-rc28",
    "rxjs": "^7.8.2",
    "snabbdom": "^3.6.2",
    "snabbdom-to-html": "^7.1.0",
    "vite": "^6.3.5"
  },
  "scripts": {
    "build:dev": "rm -rf _site && mkdir _site && cp -rf static/* _site/ && rtgl ui build-svg && rtgl fe build -d -s src/setup.web.js",
    "build": "rm -rf _site && mkdir _site && cp -rf static/* _site/ && rtgl ui build-svg && rtgl fe build -s src/setup.web.js",
    "build:web": "rm -rf _site && mkdir _site && cp -rf static/* _site/ && rtgl ui build-svg && rtgl fe build -s src/setup.web.js",
    "build:tauri": "rm -rf _site && mkdir _site && cp -rf static/* _site/ && rtgl ui build-svg && rtgl fe build -s src/setup.tauri.js",
    "watch": "rtgl fe watch -s src/setup.web.js",
    "watch:tauri": "rtgl fe watch -s src/setup.tauri.js",
    "serve": "bunx serve _site -p 3001",
    "lint": "bunx prettier --check src",
    "lint:fix": "bunx prettier --check --write src",
    "prepare": "husky",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:dev:win": "tauri dev --target x86_64-pc-windows-gnu",
    "tauri:build": "bun run build:tauri && tauri build",
    "tauri:build:win": "bun run build:tauri && tauri build --target x86_64-pc-windows-gnu"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2"
  },
  "lint-staged": {
    "src/**/*.{js}": [
      "bunx prettier --write"
    ]
  }
}
