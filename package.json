{
  "type": "module",
  "dependencies": {
    "@rettangoli/fe": "^0.0.7-rc6",
    "esbuild": "^0.25.5",
    "immer": "^10.1.1",
    "js-yaml": "^4.1.0",
    "jsdom": "^26.1.0",
    "nanoid": "^5.1.5",
    "route-engine-js": "^0.0.2-rc9",
    "route-graphics": "^0.0.2-rc8",
    "rxjs": "^7.8.2",
    "snabbdom": "^3.6.2",
    "snabbdom-to-html": "^7.1.0",
    "vite": "^6.3.5"
  },
  "scripts": {
    "build": "rm -rf _site && mkdir _site && cp -rf static/* _site/ && rtgl fe build && rtgl ui build-svg",
    "watch": "rtgl fe watch",
    "lint": "bunx prettier --check src",
    "lint:fix": "bunx prettier --check --write src",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2"
  },
  "lint-staged": {
    "src/**/*.{js}": [
      "bunx prettier --write"
    ]
  }
}
